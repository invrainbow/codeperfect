import { LINKS } from "@/constants";

It's a throwback to an era when software was way <a
href={LINKS.oldSoftwareOpenedInstantly}>faster</a>, despite running on hardware
much slower than a phone today.

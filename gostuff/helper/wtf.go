package helper

/*
import (
	"fmt"
	"github.com/denormal/go-gitignore"
)

func main() {
    ign, err := gitignore.NewRepository("/Users/brandon/ide/api")
	if err != nil {
        panic(err)
	}
    shouldIgnore := false
    match := ign.Match("/Users/brandon/ide/api/db")
    if match != nil {
        shouldIgnore = true
    }
	fmt.Printf("%v\n", shouldIgnore)
}
*/

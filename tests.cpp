#include "tests.hpp"

#include "debugger.hpp"
#include "common.hpp"
#include "world.hpp"
#include "nvim.hpp"

bool run_tests() {
    return false;

    world.init(true);
    Go_Index index;
    // if (!index.init()) return false;eturn;
    system("pause");
    return true;
}
